package api.test;

import api.helpers.reques.PlayerRequestGen;
import api.helpers.additionalReq.AdditionalRequest;

public class TestBase {
    AdditionalRequest restConst = new AdditionalRequest();
    PlayerRequestGen newPlayer = new PlayerRequestGen();
}
